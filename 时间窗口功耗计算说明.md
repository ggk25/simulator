# 时间窗口功耗计算设计说明

## 1. 背景与目标

### 原有方法
- 收集每个算子的动态能耗和执行时间
- 平均功耗 = 能耗 / 执行时间
- 只能得到算子级别的平均功耗，无法反映实际运行时的功耗波动

### 新需求
- 计算固定时间窗口内的功耗（例如2ms）
- 多个算子组合成一个时间窗口
- 算子跨窗口时按时间比例分配能耗
- 反映实际运行时的功耗变化曲线

## 2. Pipeline并行架构理解

### 硬件配置
- **芯片数量**: 32个
- **模型层数**: 64层（以seedoss为例）
- **每芯片层数**: ceil(64/32) = 2层

### Pipeline执行模型
```
时间 →
     Chip0    Chip1    Chip2    ...    Chip31
t0   mb0-L0   
t1   mb1-L0   mb0-L1   
t2   mb2-L0   mb1-L1   mb0-L2
t3   mb3-L0   mb2-L1   mb1-L2   ...   mb0-L31
...
```

### 关键概念
1. **Micro-batch**: 将总batch切分成多个小batch（本实现中为32个）
2. **流水线流动**: 每个芯片处理完一个micro-batch后传给下游芯片
3. **利用率**: 通过流水线保持所有芯片都在工作，提高利用率

## 3. 时间窗口功耗计算原理

### 3.1 数据输入
从 `inference.py` 收集的数据：
- **算子能耗**: 单个micro-batch在单层上的算子能耗（pJ）
- **算子延时**: 单个micro-batch在单层上的算子延时（ms）
- **阶段**: prefill 或 decode

### 3.2 时间线构建 (`build_operator_timeline`)

#### 扩展规则
原始数据 → 完整执行时间线：
1. **层数扩展**: 每个芯片处理 `layers_per_chip` 层
2. **Micro-batch扩展**: 重复 `num_microbatches` 次
3. **芯片并行**: 32个芯片同时工作，但有pipeline依赖

#### Pipeline约束
```python
# 当前micro-batch在当前芯片的开始时间
if chip_id > 0:
    # 必须等待上游芯片处理完当前micro-batch
    chip_time[chip_id] = max(chip_time[chip_id], chip_time[chip_id-1])
```

#### 示例
假设单层单micro-batch有2个算子：
- 算子A: 0.5ms, 100pJ
- 算子B: 0.3ms, 60pJ

对于2层、2个micro-batch、2个芯片：
```
Chip0:
  mb0-layer0: A(0.0-0.5ms), B(0.5-0.8ms)
  mb0-layer1: A(0.8-1.3ms), B(1.3-1.6ms)
  mb1-layer0: A(1.6-2.1ms), B(2.1-2.4ms)
  mb1-layer1: A(2.4-2.9ms), B(2.9-3.2ms)

Chip1:
  mb0-layer0: A(1.6-2.1ms), B(2.1-2.4ms)  # 等待Chip0完成mb0
  mb0-layer1: A(2.4-2.9ms), B(2.9-3.2ms)
  mb1-layer0: A(3.2-3.7ms), B(3.7-4.0ms)
  mb1-layer1: A(4.0-4.5ms), B(4.5-4.8ms)
```

### 3.3 时间窗口切分 (`compute_windowed_power`)

#### 窗口创建
```python
window_size_ms = 2.0  # 例如2ms窗口
windows = [
    Window(0.0-2.0ms),
    Window(2.0-4.0ms),
    Window(4.0-6.0ms),
    ...
]
```

#### 能耗分配
对于每个算子，计算其与每个窗口的重叠：

```python
# 算子: A(1.5-2.5ms, 200pJ)
# 窗口1: 0.0-2.0ms  → 重叠(1.5-2.0ms) = 0.5ms
# 窗口2: 2.0-4.0ms  → 重叠(2.0-2.5ms) = 0.5ms

# 能耗分配（假设功耗均匀）
窗口1能耗 = 200pJ × (0.5ms / 1.0ms) = 100pJ
窗口2能耗 = 200pJ × (0.5ms / 1.0ms) = 100pJ
```

#### 窗口功耗
```python
窗口功耗(W) = 窗口总能耗(pJ) / 窗口时长(ms) × 1e-9
```

### 3.4 功耗统计 (`analyze_power_statistics`)

输出指标：
- **平均功耗**: 所有窗口功耗的平均值
- **峰值功耗**: 最大窗口功耗
- **最低功耗**: 最小窗口功耗
- **功耗标准差**: 反映功耗波动程度
- **总能耗**: 所有窗口能耗之和（焦耳）
- **总时长**: 总执行时间（毫秒）

## 4. 实现细节

### 4.1 静态功耗处理
除了动态能耗，还考虑了SRAM泄漏功耗：
```python
leakage_energy_pj = leakage_power_w × duration_ms × 1e9
total_energy = dynamic_energy + leakage_energy
```

### 4.2 数据结构

#### OperatorExecution
```python
class OperatorExecution:
    name: str              # 算子名称
    start_time_ms: float   # 开始时间
    duration_ms: float     # 持续时间
    energy_pj: float       # 能耗
    chip_id: int           # 芯片ID
    layer_id: int          # 层ID
    microbatch_id: int     # micro-batch ID
```

#### TimeWindow
```python
class TimeWindow:
    start_ms: float                          # 窗口开始时间
    duration_ms: float                       # 窗口时长
    total_energy_pj: float                   # 窗口总能耗
    operators: List[(op, energy_in_window)]  # 窗口内算子列表
```

### 4.3 输出格式

#### 时间窗口功耗表
| 阶段 | 窗口ID | 开始时间(ms) | 结束时间(ms) | 窗口能耗(J) | 平均功耗(W) |
|------|--------|--------------|--------------|-------------|-------------|
| prefill | 0 | 0.0 | 2.0 | 0.0012 | 600.0 |
| prefill | 1 | 2.0 | 4.0 | 0.0015 | 750.0 |
| decode | 0 | 0.0 | 2.0 | 0.0008 | 400.0 |

#### 功耗统计表
| Prefill平均功耗(W) | Prefill峰值功耗(W) | Decode平均功耗(W) | Decode峰值功耗(W) | ... |
|--------------------|--------------------|--------------------|-------------------|-----|
| 650.0 | 800.0 | 420.0 | 500.0 | ... |

## 5. 使用方法

### 5.1 基本使用
代码已集成到 `inference.py`，运行仿真即可自动计算：
```bash
python model_inference/inference.py
```

### 5.2 调整时间窗口
在 `inference.py` 中修改：
```python
window_size_ms = 2.0  # 改为其他值，如1.0ms或5.0ms
```

### 5.3 查看结果
生成的Excel文件包含两个新工作表：
1. **时间窗口功耗**: 每个时间窗口的详细功耗
2. **功耗统计**: prefill和decode的功耗汇总统计

## 6. 注意事项

### 6.1 假设条件
1. **功耗均匀假设**: 算子执行期间功耗均匀分布
2. **完美流水线**: 不考虑数据传输延迟和同步开销
3. **芯片同质**: 所有芯片性能相同

### 6.2 局限性
1. 实际硬件可能有功耗突变（如时钟门控）
2. 未考虑温度对功耗的影响
3. 未考虑DVFS（动态电压频率调整）

### 6.3 扩展方向
1. 支持非均匀功耗模型
2. 添加芯片间通信能耗
3. 支持更复杂的并行策略（如张量并行）

## 7. 验证方法

### 7.1 能量守恒验证
```
所有窗口能耗之和 ≈ 原始算子能耗之和 × 层数 × micro-batch数
```

### 7.2 时间一致性验证
```
最后一个窗口结束时间 ≈ 原始总延时 × 层数（考虑流水线）
```

### 7.3 功耗合理性
```
平均功耗 × 总时间 ≈ 总能耗
```
